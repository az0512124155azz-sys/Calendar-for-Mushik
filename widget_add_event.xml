<?xml version="1.0" encoding="utf-8"?>
<!-- FrameLayout שקוף שממלא את כל תא הווידג'ט (שלא תמיד מרובע), עם כפתור עגול קבוע ומרוכז -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageButton
        android:id="@+id/widgetAddButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center"
        android:background="@drawable/bg_widget_circle"
        android:src="@android:drawable/ic_input_add"
        android:scaleType="centerInside"
        android:padding="14dp"
        android:elevation="6dp"
        android:contentDescription="@string/add_event_title" />

</FrameLayout>
